"""
created new repository [repo_v3]
then cloned it by using commnad [ git clone <https path>]
cloned by git bash terminal.
main branch automatically will create on remote and local.
i hv created new .py file in local main branch.
now need to add, commit and publish.
first click on git tab which is on left side tool bar
for add file click on plus symbol [staging area]
then type comment [progress of the file]
then need to push file.
now file on same branch on remote.
after file create and push it on remote then main branch will seek.
"""